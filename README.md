 **LumoTools**
High-speed Discord automation suite. Everything is multi-threaded and styled in seafoam green. This repo contains the compiled .exe and the core tools for webhook and token management.

 Features
Webhook Kaboom: Rapid-fire spammer for webhooks. Supports custom names, avatars, and message rounds.

Token Joiner: Joins servers using account tokens. Includes a User-Agent bypass to lower the risk of getting flagged.

Token Checker: Checks if your tokens are still alive. Grabs account info like username, ID, and verification status.

Server Raider: Multi-token and multi-channel support. Blast multiple channels using a list of tokens at once. Customizable delay.

 **Getting Started**
Download: Grab the LumoTools.exe from the file list above.

Run: Open the exe. Might have to install required python libs (it does for you)

Use: Follow the on-screen menu. Everything is numbered 1-4.

 Usage Tips
Multiple Tokens: In the Raider, separate your tokens with commas and an space after.

Channel IDs: Same for channels. Just copy the IDs and paste them in with commas and a space (like ", ")

Delays: Enter 0 for the fastest possible raid, or enter a number (like 0.5 or 1) if you want to bypass stricter rate limits.

Getting Tokens: 1. Open Discord in your browser.
2. Press Ctrl+Shift+C, or others (depends on browser).
3. Paste this: (()=>{let token;for(let e of Object.values(webpackChunkdiscord_app.push([[Symbol()],{},e=>e.c])))try{if(!e.exports||e.exports===window)continue;if(e.exports?.getToken)token=e.exports.getToken();for(let t in e.exports)if(e.exports?.[t]?.getToken&&"IntlMessagesProxy"!==e.exports[t][Symbol.toStringTag])token=e.exports[t].getToken()}catch{}return token;})() into the console.
4. Hit Enter and copy the token it gives you.

⚠️ *Disclaimer*
Don't be stupid. This is for educational and testing purposes. I'm not responsible for what you do with it or any banned accounts.

👑 **LumoTools**
On Top.
